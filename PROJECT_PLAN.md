# P&M 내부 매출관리 프로그램 프로젝트 계획

> Phase 2 확정 기준(2026-07-11): UI의 주 엔터티는 반려견이며 데이터 관계는 `Customer → Dog → Sale`을 유지한다. 보호자 없는 반려견을 허용하고, localStorage는 운영 저장소로 사용하지 않는다. Phase 2는 Supabase Auth, PostgreSQL, RLS, 실제 CRUD로 전환하되 승인된 Phase 1 UI는 재설계하지 않는다.

## 1. 문서 목적

이 문서는 P&M 내부 매출관리 프로그램의 고정 개발 기준을 정의한다. Phase 0 승인 이후의 모든 구현은 이 문서와 함께 `DATABASE_SCHEMA.md`, `UI_STRUCTURE.md`, `ROUTING_FLOW.md`, `COMPONENT_STRUCTURE.md`, `DEVELOPMENT_ROADMAP.md`, `TEST_CHECKLIST.md`를 기준으로 진행한다.

## 2. 현재 프로젝트 상태

- 현재 작업공간에는 애플리케이션 소스와 패키지 설정이 없다.
- 존재하는 폴더는 `work/`, `outputs/`뿐이다.
- React 컴포넌트, 페이지, 데이터베이스 마이그레이션은 아직 생성하지 않는다.
- Phase 0에서는 설계 문서만 작성한다.

## 3. 제품 목표

- P&M 직원이 매일 사용하는 내부 매출관리 프로그램을 만든다.
- 기존 엑셀 기반 상품·매출 관리 업무를 프로그램으로 완전히 대체한다.
- 화려함보다 직관성, 조회 속도, 안정성, 데이터 무결성을 우선한다.
- 최소 클릭으로 조회하고, 필요한 경우에만 입력 화면으로 이동한다.
- 최초 승인된 범위를 끝까지 일관되게 구현한다.

## 4. 확정된 업무 범위

### 4.1 사업부

사업부는 다음 3개 고정값만 사용한다.

1. 유치원
2. 교육센터
3. 호텔

사업부의 생성, 이름 변경, 삭제 기능은 제공하지 않는다.

### 4.2 상품 분류

- 관리자가 사업부별 상품 분류를 생성·수정·삭제할 수 있다.
- 생성된 분류는 데이터베이스에 저장하고 재사용한다.
- 이미 상품에서 사용 중인 분류는 삭제할 수 없다. 먼저 연결된 상품을 다른 분류로 이동해야 한다.
- 감사 이력과 참조 안정성을 위해 실제 삭제는 비활성화 방식으로 처리한다.

### 4.3 상품

- 등록 순서: 사업부 선택 → 상품 분류 선택 또는 생성 → 상품명 입력 → 기본 판매가 입력 → 저장.
- 상품은 반드시 선택한 사업부에 속한 분류를 사용한다.
- 매출에 사용되지 않은 상품도 기본적으로 비활성화 처리하며, 물리 삭제는 제공하지 않는다.
- 이미 매출에 사용된 상품은 반드시 비활성화만 허용한다.

### 4.4 매출

- 사업부를 선택하면 해당 사업부의 활성 상품만 표시한다.
- 상품을 선택하면 현재 기본 판매가를 자동 입력한다.
- 실제 판매가는 매출 등록 시 수정할 수 있다.
- 저장된 매출에는 당시 상품명, 분류명, 사업부명, 기본 판매가와 실제 판매가의 스냅샷을 보존한다.
- 상품명이나 가격이 이후 변경되어도 과거 매출 기록은 바뀌지 않는다.

## 5. Phase 0에서 확정하는 기술 기준

- 프런트엔드: React + TypeScript
- 빌드 도구: Vite
- 라우팅: React Router
- 서버 상태 및 캐시: TanStack Query
- 폼/검증: React Hook Form + Zod
- 데이터베이스 및 인증: Supabase(PostgreSQL, Auth)
- 스타일: 전역 디자인 토큰과 공용 UI 컴포넌트를 사용하는 단일 CSS 체계
- 날짜/시간 저장: UTC `timestamptz`, 화면 표시는 Asia/Seoul
- 금액 저장: 원화 정수 `bigint`, 소수점 사용 금지

라이브러리는 실제 초기 설정 단계에서 호환 가능한 안정 버전을 선택한다. 새 의존성은 명확한 필요가 있을 때만 추가한다.

## 6. 권한 기준

최소 권한 모델은 다음 2개 역할로 고정한다.

| 역할 | 조회 | 매출 등록·수정 | 상품·분류 관리 | 사용자 역할 관리 |
|---|---:|---:|---:|---:|
| 직원 `staff` | 가능 | 가능 | 불가 | 불가 |
| 관리자 `admin` | 가능 | 가능 | 가능 | 가능 |

Phase 2 세부 권한:

- 직원: 반려견·보호자 조회/등록/수정, 매출 등록, 전체 매출 조회, 본인이 등록한 정상 매출 수정.
- 관리자: 직원 권한 전체와 상품·분류 관리, 환불·취소, 목표 관리, 월 마감/해제, 사용자 권한 관리.
- 마감된 월은 역할과 관계없이 일반 매출 변경을 DB에서 차단한다.

- 로그인한 내부 사용자만 데이터에 접근할 수 있다.
- 관리자 권한은 Supabase의 사용자 프로필 테이블과 RLS 정책으로 판정한다.
- 화면에서 버튼을 숨기는 것만으로 권한을 보장하지 않고 데이터베이스 정책에서도 차단한다.

## 7. 품질 원칙

- 정상 동작하는 기존 기능을 임의로 리팩터링하거나 삭제하지 않는다.
- 한 기능의 구현과 테스트가 끝나기 전에 다음 기능을 시작하지 않는다.
- 기능 완료마다 `npm run lint`, `npm run typecheck`, `npm run build`를 순서대로 통과한다.
- 핵심 업무 규칙은 UI 검증뿐 아니라 데이터베이스 제약 조건과 RLS로도 보호한다.
- 목록은 조회가 우선이며, 검색·필터·정렬 상태가 명확해야 한다.
- 파괴적 작업은 확인 절차를 두고, 삭제 대신 비활성화를 기본으로 한다.
- 기능이 모두 완성되기 전에는 Netlify 배포를 시도하지 않는다.

## 8. 승인 및 변경 관리

- Phase 0 문서 승인 전에는 기능 구현을 시작하지 않는다.
- 승인 이후 범위나 업무 규칙 변경이 필요하면 먼저 관련 설계 문서를 수정하고 승인을 받는다.
- 개발 중 떠오른 아이디어는 임의로 구현하지 않는다.
- 각 기능 완료 보고에는 변경 파일, 구현 내용, 테스트 결과를 포함한다.

## 9. Phase 0 완료 조건

- 7개 설계 문서가 서로 모순 없이 작성되어 있다.
- 데이터 구조, 화면, 경로, 컴포넌트, 개발 순서, 테스트 기준이 연결되어 있다.
- 미확정 업무 범위는 가정으로 명확하게 표시되어 있다.
- 사용자의 문서 승인 전까지 구현 파일을 생성하지 않는다.
