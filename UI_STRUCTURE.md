# 전체 화면 구조 설계

> Phase 2 확정 기준(2026-07-11): `고객·반려견 관리`는 `반려견 관리`로 대체한다. 목록·검색·상세의 기준은 반려견이며 보호자 정보는 반려견 상세 안에 포함한다. 검색은 반려견명, 보호자명, 연락처를 지원한다. 매출 등록 순서는 `사업부 → 반려견 → 보호자 자동 표시 → 상품 분류 → 상품 → 가격 → 저장`이다. 기존 Phase 1 블루 UI와 화면 배치는 유지한다.

반려견 상세 필수 정보: 보호자/연락처, 견종, 성별, 생년월일, 체중, 메모, 누적 이용금액, 최근 이용일, 이용 횟수, 이용 사업부 비율, 전체 매출 내역. 보호자가 없으면 `보호자 정보 없음`으로 표시하며 등록과 매출을 막지 않는다.

## 1. UX 원칙

- 첫 화면은 입력이 아니라 조회 중심의 매출 목록이다.
- 자주 쓰는 기능은 한 번의 명확한 클릭으로 진입한다.
- 목록 필터는 화면 상단에 고정된 순서로 제공한다.
- 저장, 취소, 비활성화 등 행동의 위치와 표현을 전 화면에서 통일한다.
- 데스크톱 업무 환경을 우선하되 작은 화면에서도 핵심 작업이 막히지 않게 한다.
- 사용자가 현재 사업부·필터·활성 상태를 즉시 알 수 있게 한다.

## 2. 전체 정보 구조

```text
로그인
└─ 앱
   ├─ 매출 관리
   │  ├─ 매출 목록(기본 화면)
   │  ├─ 매출 등록
   │  └─ 매출 수정
   ├─ 상품 관리 [관리자]
   │  ├─ 상품 목록
   │  ├─ 상품 등록
   │  └─ 상품 수정
   ├─ 상품 분류 관리 [관리자]
   └─ 사용자 관리 [관리자]
```

## 3. 공통 앱 셸

### 데스크톱

- 왼쪽 사이드바: 로고/프로그램명, 매출 관리, 상품 관리, 상품 분류, 사용자 관리.
- 관리자 전용 메뉴는 관리자에게만 표시한다.
- 상단 헤더: 현재 페이지명, 로그인 사용자명, 로그아웃.
- 본문: 페이지 제목, 주요 행동, 필터/폼, 결과 영역 순서.

### 작은 화면

- 사이드바는 메뉴 버튼으로 열고 닫는다.
- 표는 필수 열을 우선 노출하고 가로 스크롤을 허용한다.
- 저장 버튼은 폼 하단에서 충분한 터치 영역을 제공한다.

## 4. 화면별 구조

### 4.1 로그인

목적: 허가된 내부 직원만 프로그램에 접근한다.

- 이메일
- 비밀번호
- 로그인 버튼
- 인증 실패 메시지

회원가입과 공개 비밀번호 재설정은 초기 범위에 포함하지 않는다. 사용자 계정은 관리자가 관리한다.

### 4.2 매출 목록

앱 진입 후 기본 화면이다.

상단:

- 페이지 제목 “매출 관리”
- 주요 버튼 “매출 등록”

필터:

- 기간: 시작일, 종료일
- 사업부: 전체, 유치원, 교육센터, 호텔
- 상품: 사업부 선택에 따라 목록 제한
- 검색: 상품명과 메모 대상
- 초기화 버튼

요약:

- 조회 건수
- 조회 기간 총매출

표:

- 매출일
- 사업부
- 상품 분류
- 상품명
- 기본 판매가
- 실제 판매가
- 등록자
- 메모
- 수정

기본 정렬은 매출일 내림차순, 같은 날짜에서는 생성 시각 내림차순이다. 빈 결과, 로딩, 오류 상태를 각각 제공한다.

### 4.3 매출 등록

업무 순서와 동일한 단일 폼으로 구성한다.

1. 매출일(기본값: 오늘, Asia/Seoul)
2. 사업부
3. 상품(사업부 선택 전 비활성)
4. 상품 분류(상품 선택 후 읽기 전용 표시)
5. 기본 판매가(자동 표시, 읽기 전용)
6. 실제 판매가(상품 선택 시 기본 판매가 자동 입력, 수정 가능)
7. 메모(선택)
8. 저장 / 취소

- 사업부를 바꾸면 기존 상품 선택과 가격을 초기화한다.
- 중복 저장 방지를 위해 저장 중 버튼을 잠근다.
- 저장 성공 후 매출 목록으로 이동하고 성공 메시지를 표시한다.

### 4.4 매출 수정

- 등록 폼과 같은 필드 순서를 사용한다.
- 기존 저장값을 표시한다.
- 상품 변경 시 기본 판매가와 스냅샷을 새 상품 기준으로 갱신한다.
- 취소 시 변경 없이 목록으로 돌아간다.
- 매출 삭제는 현재 범위에 포함하지 않는다.

### 4.5 상품 목록 [관리자]

상단:

- 페이지 제목 “상품 관리”
- “상품 등록” 버튼

필터:

- 사업부
- 상품 분류(사업부 연동)
- 상태: 활성, 비활성, 전체
- 상품명 검색

표:

- 사업부
- 상품 분류
- 상품명
- 기본 판매가
- 상태
- 수정
- 활성/비활성 전환

비활성 상품은 상태 배지와 낮은 시각 강조로 구분하되 계속 조회할 수 있다.

### 4.6 상품 등록·수정 [관리자]

1. 사업부
2. 상품 분류
3. 상품명
4. 기본 판매가
5. 저장 / 취소

- 선택한 사업부의 활성 분류만 표시한다.
- 분류가 없으면 같은 화면에서 “새 분류 만들기” 보조 동작으로 작은 대화상자를 연다.
- 분류 생성 성공 후 새 분류를 자동 선택한다.
- 수정 화면에서 사업부를 바꾸면 분류 선택을 초기화한다.
- 상품 활성 상태 변경은 목록 또는 수정 화면에서 확인 후 실행한다.

### 4.7 상품 분류 관리 [관리자]

- 사업부 탭 또는 필터: 유치원, 교육센터, 호텔
- 분류명 검색
- “분류 추가” 버튼
- 분류 목록: 분류명, 연결 상품 수, 상태, 수정, 활성/비활성 전환
- 추가·수정은 간단한 대화상자 사용
- 활성 상품이 연결된 분류를 비활성화하려 하면 이유와 해결 방법을 표시하고 차단
- 물리 삭제 버튼은 제공하지 않음

### 4.8 사용자 관리 [관리자]

권한 모델을 운영하기 위한 최소 화면이다.

- 사용자명
- 이메일
- 역할
- 활성 상태
- 역할/상태 수정

계정 초대 방식은 Supabase 인증 설정 단계에서 결정하되, 공개 회원가입은 허용하지 않는다.

### 4.9 접근 거부 및 찾을 수 없음

- 권한 없음: 접근 불가 이유와 매출 목록 이동 버튼.
- 404: 찾을 수 없는 화면 안내와 매출 목록 이동 버튼.

## 5. 공통 상태 표현

- 로딩: 콘텐츠 영역과 형태가 유사한 스켈레톤 또는 명확한 진행 표시.
- 빈 결과: “조건에 맞는 매출이 없습니다”처럼 원인과 다음 행동 표시.
- 오류: 사용자 언어로 설명하고 다시 시도 버튼 제공.
- 성공: 저장 직후 짧은 알림과 목록의 갱신된 결과로 확인.
- 유효성 오류: 해당 입력 바로 아래에 표시하고 첫 오류로 초점 이동.
- 비활성 상태: 배지로 명확히 표시하고 신규 선택 목록에서는 제외.

## 6. 디자인 일관성 기준

- 공용 색상, 간격, 글꼴 크기, 테두리, 그림자 토큰을 사용한다.
- 기본 본문 16px 이상, 표와 보조문은 가독성을 해치지 않는 범위에서만 축소한다.
- 주 행동은 페이지당 하나의 강조 버튼으로 제한한다.
- 위험 행동은 경고 색상과 확인 대화상자를 사용한다.
- 키보드만으로 탐색·입력·저장이 가능해야 한다.
- 모든 입력은 연결된 라벨을 가지며 색상만으로 상태를 전달하지 않는다.
