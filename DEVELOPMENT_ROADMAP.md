# 개발 로드맵

> Phase 2 승인 변경(2026-07-11): Phase 1 UI가 승인되었으며 이제 localStorage를 Supabase로 전환한다. 순서는 `client → Auth → profile → business_units → categories → products → customers → dogs → sales/history → targets/closing → dashboard/report 집계`로 고정한다. 각 항목은 구현·테스트·오류 수정과 lint/typecheck/build 완료 후 다음 항목으로 이동한다.

Phase 2 완료 전 Netlify 배포를 금지한다. 환경변수가 없으면 migration과 로컬 기반 준비 후 실제 연결 시점에 중단하고 사용자 입력을 요청한다.

## 1. 진행 규칙

- 아래 순서를 승인 후 고정하며 임의로 건너뛰지 않는다.
- 한 단계의 한 기능만 구현한다.
- 각 기능은 구현, 기능 테스트, 오류 수정, 필수 검증 완료 후 종료한다.
- 필수 검증은 항상 다음 순서다.

```text
npm run lint
npm run typecheck
npm run build
```

- 실패하면 수정 후 세 명령을 다시 실행한다.
- 완료 보고에는 변경 파일, 구현 기능, 테스트 결과를 포함한다.
- 모든 기능 완성 전 Netlify 배포를 시도하지 않는다.

## 2. Phase 0 — 설계 확정 (현재 단계)

산출물:

- `PROJECT_PLAN.md`
- `DATABASE_SCHEMA.md`
- `UI_STRUCTURE.md`
- `ROUTING_FLOW.md`
- `COMPONENT_STRUCTURE.md`
- `DEVELOPMENT_ROADMAP.md`
- `TEST_CHECKLIST.md`

완료 조건:

- 문서 간 용어와 업무 규칙이 일치한다.
- 구현은 시작하지 않는다.
- 사용자 승인 후 Phase 1로 이동한다.

## 3. Phase 1 — 로컬 개발 기반

### 1.1 프로젝트 초기화

- Vite React TypeScript 프로젝트 설정.
- lint, typecheck, build, test 명령 구성.
- `.env.example` 작성과 환경 변수 검증.

### 1.2 공통 스타일과 최소 UI 기반

- 디자인 토큰과 전역 스타일.
- 버튼, 입력, 선택, 폼 필드, 상태 표시.
- 접근성 기본 테스트.

### 1.3 앱 셸과 라우터

- 앱 셸, 사이드바, 헤더.
- 문서에 정의된 경로만 등록.
- 아직 업무 기능은 연결하지 않는다.

각 하위 기능별로 필수 검증을 별도 통과한다.

## 4. Phase 2 — Supabase 기반과 인증

### 2.1 로컬 Supabase 스키마

- 테이블, 제약, 인덱스, 고정 사업부 seed.
- `updated_at` 트리거.
- 생성 타입 연결.

### 2.2 RLS와 권한 함수

- 활성 직원과 관리자 정책.
- 비로그인, 직원, 관리자 권한 테스트.

### 2.3 로그인과 접근 제어

- 로그인/로그아웃.
- 인증 및 관리자 경로 가드.
- 비활성 사용자 차단.

## 5. Phase 3 — 상품 분류 관리

### 3.1 분류 조회

- 고정 사업부별 분류 목록, 상태, 연결 상품 수.

### 3.2 분류 생성

- 이름 검증과 중복 차단.

### 3.3 분류 수정

- 사업부 안에서 이름 수정.

### 3.4 분류 비활성화·재활성화

- 활성 상품 연결 시 비활성화 차단.
- 과거 참조 보존 확인.

## 6. Phase 4 — 상품 관리

### 4.1 상품 조회

- 사업부·분류·상태·검색 필터.

### 4.2 상품 등록

- 확정 등록 순서 구현.
- 등록 중 새 분류 생성 및 자동 선택.

### 4.3 상품 수정

- 사업부/분류 일치와 가격 검증.

### 4.4 상품 비활성화·재활성화

- 물리 삭제 금지.
- 신규 매출 선택 목록 반영.

## 7. Phase 5 — 매출 관리

### 5.1 매출 등록

- 사업부별 활성 상품 필터.
- 기본 판매가 자동 입력과 실제 판매가 수정.
- 서버 RPC 스냅샷 저장.

### 5.2 매출 조회

- 기간·사업부·상품·검색 필터.
- 총매출과 건수.
- URL 기반 필터 유지.

### 5.3 매출 수정

- 기존 값 로드와 수정.
- 상품 변경 시 스냅샷 재계산.

## 8. Phase 6 — 사용자 관리

### 6.1 사용자 목록

- 관리자 전용 조회.

### 6.2 역할과 활성 상태 변경

- 관리자/직원 전환.
- 비활성 사용자 접근 차단.
- 관리자가 자기 자신을 실수로 비활성화하거나 마지막 관리자를 제거하지 못하게 보호한다.

## 9. Phase 7 — 통합 안정화

기능 추가 없이 다음 작업만 수행한다.

- 전체 업무 흐름 회귀 테스트.
- 데이터 정합성 및 RLS 재검증.
- 키보드 접근성과 주요 화면 반응형 확인.
- 로딩, 빈 상태, 오류, 중복 저장 방지 확인.
- 실제 엑셀 대체 시나리오 기반 사용자 확인.
- 오류 수정 후 전체 lint, typecheck, build, 자동 테스트 통과.

## 10. 배포 승인 게이트

다음 조건을 모두 만족하고 사용자가 별도로 승인한 뒤에만 배포 단계를 계획한다.

- Phase 1~7 완료.
- 미해결 중요 오류 없음.
- 전체 테스트 체크리스트 통과.
- `npm run lint`, `npm run typecheck`, `npm run build` 최종 통과.
- Supabase 운영 환경 마이그레이션과 보안 설정 검토 완료.

이 로드맵에는 Netlify 배포 실행을 포함하지 않는다.
